@model IEnumerable<TestingDatabase3.Models.RestaurantReview>

@{
    ViewBag.Title = "About";
}

@foreach (var item in Model)
{
    <div>
        <h4>@item.Id</h4>
        @item.Body
        @item.Rating
       @* @item.RestaurantId*@
    </div>
}

