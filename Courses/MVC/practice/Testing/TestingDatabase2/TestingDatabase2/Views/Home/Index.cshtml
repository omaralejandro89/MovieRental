@model IEnumerable<TestingDatabase2.Models.Restaurant>

@{
    ViewBag.Title = "Home Page";
}

@foreach (var item in Model)
{
    @item.Id
    @item.Name
    @item.City
    @item.RestaurantReviews
    <hr/>
}
